1.进入某一个文件夹
cd 某一个文件夹路径

$ git config --global user.name "用户名"
$ git config --global user.email "邮箱"
git remote add origin 你自己的git地址

2.初始化git
git init
3.将文件添加到库
git add .
4.提交文件到库
git commit -m "提交文件"
5.上传代码到指定库
这里我用了小乌龟的创建分支功能，
创建分支功能后
git push -u origin 分支名字
即可